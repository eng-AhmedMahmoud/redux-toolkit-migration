import {
    APP,
    CONTAINER_OVERLAY,
} from '../../constants';

// Actions
export const GET_OVERLAY_CONTENT = `${APP}/${CONTAINER_OVERLAY}/GET_OVERLAY_CONTENT`;
export const OPEN_OVERLAY = `${APP}/${CONTAINER_OVERLAY}/OPEN_OVERLAY`;
